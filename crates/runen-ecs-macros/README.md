# RunenECS macros

This package contains the derive macros for the standalone `runen-ecs` package.
It is maintained alongside RunenECS because Rust requires procedural macros to
be provided by a separate package.
